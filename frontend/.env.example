# Keep /api when the frontend and backend are served behind the same reverse proxy.
VITE_API_BASE_URL=/api

# If the backend is deployed on a separate domain, use that backend API prefix instead.
# VITE_API_BASE_URL=https://api.example.com
